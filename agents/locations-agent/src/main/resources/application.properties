spring.application.name=locations-agent

# Mock service catalog (run mockServices first)
locations.api.base-url=http://localhost:8082
locations.api.catalog-path=/api/v1/places

server.port=8091

# Minimum confidence (0–1) to return a fuzzy match
locations.resolve.min-confidence=0.55
